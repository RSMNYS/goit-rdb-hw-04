Коли я змінив оператори INNER на LEFT/RIGHT JOIN, я помітив збільшення кількості рядків у результатах запиту. Це відбувається тому, що:

* При використанні INNER JOIN я отримував лише ті рядки, де є збіги в обох таблицях
* Коли я замінив на LEFT JOIN, у результат включились всі рядки з лівої таблиці, навіть якщо для них не було відповідних записів у правій таблиці
* Аналогічно, при використанні RIGHT JOIN я отримав всі рядки з правої таблиці, навіть без відповідників у лівій

Збільшення кількості рядків у результаті залежало від того, скільки було записів без пар у відповідних таблицях. Тобто якщо в одній з таблиць були записи, які не мали відповідників в іншій таблиці за умовою join, вони з'явились у результатах при використанні відповідного типу LEFT або RIGHT JOIN, на відміну від INNER JOIN, де такі записи відфільтровувались.